import type { BlockDefinition } from '../types'
import { Database } from 'lucide-react'

export const lakebaseMemoryDefinition: BlockDefinition = {
  id: 'lakebaseMemory',
  label: 'Lakebase Memory',
  Icon: Database,
}
