import type { BlockDefinition } from '../types'
import { BookOpenText } from 'lucide-react'

export const knowledgeAssistantDefinition: BlockDefinition = {
  id: 'knowledgeAssistant',
  label: 'Knowledge Assistant',
  Icon: BookOpenText,
}
