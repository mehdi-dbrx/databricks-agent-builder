import type { BlockDefinition } from '../types'
import { Search } from 'lucide-react'

export const vectorSearchDefinition: BlockDefinition = {
  id: 'vectorSearch',
  label: 'Vector Search',
  Icon: Search,
}
