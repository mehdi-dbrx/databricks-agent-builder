import type { BlockDefinition } from '../types'
import { Expand } from 'lucide-react'

export const multiAgentSupervisorDefinition: BlockDefinition = {
  id: 'multiAgentSupervisor',
  label: 'Multi-Agent Supervisor',
  Icon: Expand,
}
